sudo: required
dist: trusty
language: cpp

- os: linux
  addons:
    apt:
      sources:
        - ubuntu-toolchain-r-test
      packages:
        - g++-6
  env:
    - MATRIX_EVAL="CC=gcc-6 && CXX=g++-6"

script:
  - mkdir build
  - cd build
  - cmake ..
  - make
